{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and pet overlay PoC window",
  "windows": ["main", "pet-overlay"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
